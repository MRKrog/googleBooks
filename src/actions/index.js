export const setError = (error) => ({
  type: "SET_ERROR",
  error
})

export const setLoading = (loading) => ({
  type: "SET_LOADING",
  loading
})

export const setBookSearch = (books) => ({
  type: "BOOK_SEARCH",
  books
})
